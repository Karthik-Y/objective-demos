import React from "react";

// container, hero, section, tiles, and footer.

const Layouts = () => {
    return (
        <div>
            <h1> Layout rendered </h1>
            {/* To do */}
        </div>
    );
}

export default Layouts;